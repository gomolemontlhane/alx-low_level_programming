What is the space complexity of the allocate_map function?

The allocate_map function is responsible for allocating memory for a 2D array (map) of size n x m. Let's analyze the space complexity of this function:

int **allocate_map(int n, int m)
{
    int **map;

    map = malloc(sizeof(int *) * n); // Allocate memory for array of pointers (size n)
    for (size_t i = 0; i < n; i++)
    {
        map[i] = malloc(sizeof(int) * m); // Allocate memory for each row (size m)
    }
    return map;
}
