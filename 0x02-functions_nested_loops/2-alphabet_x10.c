#include <main.h>
/**
 * main - entry point
 *
 * description: Program prints 10 times the alphabet, in lowercase.
 *
 * return - result is 0 if sucessful, without it's error.
 */

int main()
{
	void 2-alphabet_x10(void)
	{
		for (int i = 0; i < 5; i++) 
		{
			while (letter <= 'z')
			{
				_putchar(letter);
				letter++;
			}
			_putchar('\n');
		}
	}
}
