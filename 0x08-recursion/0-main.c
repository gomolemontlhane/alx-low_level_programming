#include "main.h"

/**
 * main - Check the _puts_recursion function
 *
 * Return: Always 0.
 */
int main(void)
{
	/* Use a tab character for indentation here */
	_puts_recursion("Puts with recursion");
	return (0);
}
